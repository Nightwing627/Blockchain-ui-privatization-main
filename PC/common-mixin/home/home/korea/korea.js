import { imgMap } from '@/utils';

export default {
  data() {
    return {
      qShowList: [],
      headerStyle: `background:url(${imgMap.ko1_bg}) 0 0 no-repeat`,
      koMore: imgMap.ko_more,
    };
  },
};
